# Configurações do frontend Dipedra
# CATALOG_SOURCE aceita: auto, api, local
CATALOG_SOURCE=auto
# Base para chamadas da API (deixe em branco para usar o comportamento padrão)
API_BASE_URL=
# Caminho/URL do catálogo local utilizado como fallback
FALLBACK_CATALOG_URL=assets/catalogo.json
# Base para as imagens do catálogo
IMAGE_BASE_URL=assets/images/catalogo/
# Imagem utilizada como placeholder
PLACEHOLDER_IMAGE_URL=assets/images/placeholder.jpg
